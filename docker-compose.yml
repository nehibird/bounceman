services:
  web:
    build: .
    ports:
      - "3202:3200"
    env_file: .env
    environment:
      - DB_PATH=/app/data/bounceman.db
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    restart: unless-stopped
